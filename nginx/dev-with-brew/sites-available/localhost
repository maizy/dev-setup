server {
    listen       80 default;
    server_name  localhost;

    location / {
        root   /opt/www;
        autoindex on;
    }

    location = /50x.html {
        root   html;
    }

    location /status {
        stub_status;
    }

    location ~ /\. {
        deny  all;
    }
}
